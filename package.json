{
  "name": "effect.js",
  "version": "0.0.1",
  "description": "Composable Side-Effects for JavaScript",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compress": "./node_modules/uglify-js/bin/uglifyjs dist/effect.js --in-source-map dist/effect.js.map --compress --mangle --output dist/effect.js.min --source-map dist/effect.js.min.map",
    "transpile": "babel lib --out-file dist/effect.js --source-maps",
    "build": "npm run transpile && npm run compress"
  },
  "author": "Viktor Kronvall",
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^1.3.3",
    "whatwg-fetch": "^0.10.1",
    "zen-observable": "^0.1.9"
  },
  "devDependencies": {
    "uglify-js": "^2.6.1"
  }
}
